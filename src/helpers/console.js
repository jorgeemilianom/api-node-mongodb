
const header = (message) => {
    // console.clear();
    console.log('=========================================================');
    console.log(`|  ${message}`);
    console.log('=========================================================');
}

module.exports = {
    header,
};